from crewai import Task 

class Main_Tasks():
    def manage_whole_task(self, agent):
        return Task(
            description=(
                
            ),
            expected_output=(
               
            ),
            agent=agent,
            context=context
        )

    def re_architect_graph(self, agent, context):
        return Task(
            description=(
                
            ),
            expected_output=(
                
            ),
            agent=agent,
            context=context
        )
    
    def refine_prompts(self, agent, context):
        return Task(
            description=(
                
            ),
            expected_output=(
                
            ),
            agent=agent,
            context=context
        )
    
